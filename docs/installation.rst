============
Installation
============

At the command line::

    $ easy_install django-bcm

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-bcm
    $ pip install django-bcm
